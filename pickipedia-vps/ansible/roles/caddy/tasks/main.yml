---
# Caddy for automatic SSL and reverse proxy
# Much simpler than certbot + nginx SSL config

- name: Install Caddy dependencies
  apt:
    name:
      - debian-keyring
      - debian-archive-keyring
      - apt-transport-https
    state: present

- name: Add Caddy GPG key
  apt_key:
    url: https://dl.cloudsmith.io/public/caddy/stable/gpg.key
    state: present

- name: Add Caddy repository
  apt_repository:
    repo: "deb https://dl.cloudsmith.io/public/caddy/stable/deb/debian any-version main"
    state: present
    filename: caddy-stable

- name: Install Caddy from apt (provides systemd unit and caddy user)
  apt:
    name: caddy
    state: present
    update_cache: yes

- name: Pin Caddy to 2.6.2 (newer versions drop Firefox XHR POST bodies)
  shell: |
    CURRENT=$(caddy version 2>/dev/null | cut -d' ' -f1)
    if [ "$CURRENT" != "v2.6.2" ]; then
      systemctl stop caddy || true
      curl -fsSL "https://caddyserver.com/api/download?os=linux&arch=amd64&version=v2.6.2" -o /usr/bin/caddy
      chmod +x /usr/bin/caddy
      systemctl start caddy
      echo "changed"
    else
      echo "ok"
    fi
  register: caddy_pin
  changed_when: "'changed' in caddy_pin.stdout"

- name: Hold Caddy apt package to prevent auto-upgrade
  dpkg_selections:
    name: caddy
    selection: hold

- name: Create Caddy log directory
  file:
    path: /var/log/caddy
    state: directory
    owner: caddy
    group: caddy
    mode: '0755'

- name: Create Caddyfile
  copy:
    dest: /etc/caddy/Caddyfile
    content: |
      # PickiPedia - SSL termination and reverse proxy
      # Caddy automatically handles Let's Encrypt certificates

      {{ domain_name }}, {{ www_domain }} {
          reverse_proxy 127.0.0.1:8080
      }

      # Health check endpoint (for monitoring from maybelle)
      :8081 {
          respond /health "OK" 200
      }
    mode: '0644'
  notify: Restart Caddy

- name: Start and enable Caddy
  systemd:
    name: caddy
    state: started
    enabled: yes
  register: caddy_start
  ignore_errors: yes

- name: Check Caddy status if start failed
  command: journalctl -u caddy --no-pager -n 20
  register: caddy_journal
  when: caddy_start.failed | default(false)

- name: Show Caddy error
  debug:
    var: caddy_journal.stdout_lines
  when: caddy_start.failed | default(false)

- name: Fail if Caddy didn't start
  fail:
    msg: "Caddy failed to start. Check the journal output above."
  when: caddy_start.failed | default(false)
